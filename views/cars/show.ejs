<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <h1 class="animate__animated animate__fadeIn">Hello, <%= user ? user.profile.name : "stranger" %></h1>
  <h2 class="animate__animated animate__fadeIn">Like my car? Leave a comment and hit the like button 😉</h2>


<div id="single-car" class="container">
  <div>I drive a <%= car.year %>, <%= car.color %> <%= car.brand %> <%= car.model %>. It's a <%= car.feedback %>. <br> Right not it's <%= car.forSale ? "for sale" : "not for sale"%>. <br> My dream car is - <%= car.dream %>
    <div class="driver-fedback"> Few words from <%= car.driver.name %> about this car:
      <p><%= car.driverNote %></p>
    </div>
  <div class="likes"> Comments <br>
    0 people like this car
  </div>
  <div></div>
  <br>
  <% if(user) { %>
  <form action="/cars/<%= car._id %>?_method=PUT" method="POST">
    <div class="comments-text">
      <label for="user-comments">Write your comment here:</label>
      <br>
      <textarea name="comment" id="user-comments"></textarea>
      <br>
      <button>Comment</button>
  </div>
  
    <a class="google" href="">
      <button id="logout" type="submit">Like my Ride!</button>
    </a>
  </form>
  <% } %>
</div>

<% if(user) {%> 
  <% if (car.driver.equals(user.profile._id)) { %> 
    <form class="google" action="/cars/<%= car._id %>?_method=DELETE" method="POST">
        <button  id="logout" type="submit">Delete</button>
    </form>
  <a class="google" href="/cars/<%= car._id %>/edit">
    <button id="logout" type="submit">Edit</button>
  </a>
  <% } else { %> 

  <% }} %> 
</div>


</main>

<%- include('../partials/footer') %>